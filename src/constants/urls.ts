const BASE_URL = "https://api.thecatapi.com";

const URLs = {
  images: `${BASE_URL}/v1/images`,
  favourites: `${BASE_URL}/v1/favourites`,
  upload: `${BASE_URL}/v1/images/upload`,
};

export default URLs;
